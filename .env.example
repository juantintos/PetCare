# Copia este archivo como .env y ajusta los valores.
# Expo solo expone al bundle las variables prefijadas con EXPO_PUBLIC_.
# NUNCA pongas secrets reales aquí (API keys privadas, credenciales, etc.).

EXPO_PUBLIC_API_URL=https://mock.petcare.local/api
EXPO_PUBLIC_USE_MOCK_API=true
